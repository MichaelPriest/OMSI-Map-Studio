# OMSI Map Studio v0.2.0-alpha.5-test.10.54-native

## Test 10.54 — visual terrain brush in the viewport

This preview makes terrain editing more viewport-first:

- brush circle projected onto terrain under the pointer;
- real meter radius follows camera/zoom;
- inner ring shows the non-feathered area;
- outer area represents the feather transition;
- compact HUD shows current radius and feather;
- Inspector radius/feather changes update the preview immediately;
- **Brush** command in the Terrain contextual toolbar;
- persistent terrain-point picking while the module is active;
- tested Renderer world-to-screen projection.

Level/raise/lower/groundtex persistence continues to reuse the existing real handlers and OMSI writers.

> PR #3 remains DRAFT. No merge into main was performed.
