# OMSI Map Studio v0.2.0-alpha.5-test.10.48-native

Bidirectional synchronization between the viewport and traffic-light programs.

## Map → program

- clicking a traffic-light object while Traffic mode is active resolves the corresponding program;
- matching uses the real ObjectId and Tile X/Y;
- the matching program is selected and scrolled into view;
- synchronization avoids cycles when the list and viewport already point to the same program.

## Program → map

The Test 10.47 behavior remains: selecting a program still selects and frames the corresponding real scene object in the viewport.

## State

- branch: `feature/native-winui-d3d11`;
- PR #3 remains DRAFT;
- `main` remains untouched.
