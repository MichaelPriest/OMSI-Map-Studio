# OMSI Map Studio v0.2.0-alpha.5-test.10.52-native

## Test 10.52 — textura de terreno por Custom Shape

Esta preview amplia o fluxo visual do editor para o terreno:

- captura de polígono diretamente no viewport;
- preenchimento real de `groundtex` dentro da forma;
- suporte a polígonos côncavos;
- pintura atravessando múltiplos tiles OMSI;
- suavização de borda usando os controles atuais de raio/suavização;
- persistência real dos masks DDS e recarregamento do mapa;
- teste de regressão no Core para impedir preenchimento do recorte côncavo.

A implementação reutiliza os dados reais do mapa e os writers OMSI existentes. Não há mock.

> PR #3 continua DRAFT. Esta versão não faz merge na main.
